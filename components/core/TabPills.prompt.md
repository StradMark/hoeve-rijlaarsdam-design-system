One-line: audience/segment filter pills that sit under a section intro and swap the gallery below.

```jsx
<TabPills items={['Zakelijk','Catering','Particulier']} value={tab} onChange={setTab} />
```

Selected pill = deep-green fill, white uppercase serif. Unselected = white pill with green text on the sage band.
