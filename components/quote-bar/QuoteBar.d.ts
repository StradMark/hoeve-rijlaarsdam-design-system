import * as React from 'react';
/**
 * Full-bleed deep-green band holding one centred line in letterspaced display-serif capitals —
 * the same treatment as RIJLAARSDAM in the logo. Sits directly under the hero on every page.
 */
export interface QuoteBarProps { children?: React.ReactNode; style?: React.CSSProperties }
export declare function QuoteBar(props: QuoteBarProps): JSX.Element;
